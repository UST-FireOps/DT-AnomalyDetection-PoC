from src.logger import logging
from datetime import datetime

class DataTransformation:
    def dataTransformationMethod():
        logging.info(f"Data Transformation just started at {(datetime.now())}")


